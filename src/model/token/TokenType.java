package model.token;

public enum TokenType {

    Main, Const, Int, Return, If, Else, While, Continue, Break,
    LPar, RPar, LBrace, RBrace, Semicolon, Comma,
    Add, Sub, Mul, Div, Mod, Assign,
    Eq, Lt, Lte, Gt, Gte, Not, Neq, And, Or,
    Ident, Number, Dec, Oct, Hex,
    Comment

}
